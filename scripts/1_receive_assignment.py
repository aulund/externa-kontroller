import os\nimport shutil\nimport pandas as pd\n\n# Prompting user for data\nprogram_name = input("Enter program name: ")\nyear = input("Enter year: ")\nreceived_date = input("Enter received date: ")\nsender_organization = input("Enter sender organization: ")\nassignment_description = input("Enter assignment description: ")\ndeadline = input("Enter deadline: ")\npriority_level = input("Enter priority level: ")\nnumber_of_samples = input("Enter number of samples: ")\nsample_list = input("Enter sample list (comma-separated): ")\nexpected_results = input("Enter expected results: ")\nreference_version = input("Enter reference version: ")\nassignment_pdf_path = input("Enter path to assignment PDF file: ")\nassigned_chemist_name = input("Enter assigned chemist name: ")\nnotes = input("Enter any notes: ")\n\n# Creating directory structure\nbase_dir = f'programs/{program_name}/{year}/pdfs'\nos.makedirs(base_dir, exist_ok=True)\n\n# Preparing data for CSV and Markdown\ndata = {\n    'Program Name': program_name,\n    'Year': year,\n    'Received Date': received_date,\n    'Sender Organization': sender_organization,\n    'Assignment Description': assignment_description,\n    'Deadline': deadline,\n    'Priority Level': priority_level,\n    'Number of Samples': number_of_samples,\n    'Sample List': sample_list,\n    'Expected Results': expected_results,\n    'Reference Version': reference_version,\n    'Assigned Chemist Name': assigned_chemist_name,\n    'Notes': notes\n} \n\n# Creating CSV file\ncsv_path = f'programs/{program_name}/{year}/1_assignment.csv'\ndf = pd.DataFrame([data])\ndf.to_csv(csv_path, index=False)\n\n# Creating Markdown file\nmd_path = f'programs/{program_name}/{year}/1_assignment.md'\nwith open(md_path, 'w') as md_file:\n    md_file.write(f"# Assignment Details for {program_name}\n\n")\n    for key, value in data.items():\n        md_file.write(f"**{key}:** {value}\n\n")\n\n# Copying the PDF file\nshutil.copy(assignment_pdf_path, base_dir)\n\n# Confirmation message\nprint(f"Created files:\n- {csv_path}\n- {md_path}\n- PDF copied to {base_dir}/")\n